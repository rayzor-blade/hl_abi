[package]
name = "hl_abi"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "The HashLink C ABI, as a native library sees it"
publish = false

# None, and there must not be any. See the crate docs.
[dependencies]
